<%= form_for(@site_contact) do |f| %>
  <% if @site_contact.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@site_contact.errors.count, "error") %> prohibited this site_contact from being saved:</h2>

      <ul>
      <% @site_contact.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<table>

  <table class="forum_view">
	<tr>
    <div class="field">
	<th>Site</th>
	<td><%= collection_select(:site_contact, :site_id, Site.all, :id, :name) %></td>
  </div>
</tr>
<tr>
<div class="field">
	<th>Contact</th>
	<td><%= collection_select(:site_contact, :contact_id, Contact.all, :id, :name) %></td>
  </div>
</tr>
<tr>
  <div class="actions">
    <td><%= f.submit %></td>
  </div>
</tr>
<% end %>
</table>